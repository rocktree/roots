<%= simple_form_for @item, :url => @url do |f| %>

  <%= form_section @model.to_s.humanize.downcase do %>
    <% @model.column_names.each do |attribute| %>
      <%= f.input attribute.to_sym if form_eligible?(attribute) %>
    <% end %>
  <% end %>

  <%= f.button :submit %>

<% end %>
