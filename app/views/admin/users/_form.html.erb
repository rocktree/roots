<%= simple_form_for @item, :url => @url do |f| %>

  <%= form_section 'user info' do %>
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.input :password_confirmation %>
    <%= f.input :is_admin, :label => 'Admin?' %>
  <% end %>

  <%= f.button :submit %>

<% end %>